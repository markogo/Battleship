@page
@model WebApp.Pages.PlayerBoardStates.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PlayerBoardState</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PlayerBoardState.PlayerBoardStateId" />
            <div class="form-group">
                <label asp-for="PlayerBoardState.PlayerId" class="control-label"></label>
                <select asp-for="PlayerBoardState.PlayerId" class="form-control" asp-items="ViewBag.PlayerId"></select>
                <span asp-validation-for="PlayerBoardState.PlayerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PlayerBoardState.CreatedAt" class="control-label"></label>
                <input asp-for="PlayerBoardState.CreatedAt" class="form-control" />
                <span asp-validation-for="PlayerBoardState.CreatedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PlayerBoardState.BoardState" class="control-label"></label>
                <input asp-for="PlayerBoardState.BoardState" class="form-control" />
                <span asp-validation-for="PlayerBoardState.BoardState" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
